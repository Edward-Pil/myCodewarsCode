var str='asdasad';

function solution(str){
    if(str.length% 2 === 0){
        return str.match(/..?/g);
    }
    else{ 
        str= str+ '_';
        return str.match(/.{1,2}/g);
    }
}


var fn = solution(str);
console.log (fn);